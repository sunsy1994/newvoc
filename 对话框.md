产品形态：一个 AI 对话框组件（Chat UI），支持 3 个场景入口：问数 / 问答 / 生成报告。

目标：

用户在“悬停”时能感受到组件可交互、可探索（更强的引导与反馈）。

用户点击对话框后，能快速选择场景（带动效、体验酷炫但不打扰输入）。

输入问题后，对话区域呈现具有“AI 思考/推理”的酷炫效果（强调智能与在处理）。

2. 核心交互流程
2.1 默认态（未悬停）

展示一个简洁的对话框容器（类似 Chat 卡片）。

仅展示：

Placeholder：例如“输入问题，或选择一个场景开始…”

轻量提示：如右上角一个“模式/场景”小图标（不抢夺注意力）

2.2 悬停态（Hover）

悬停触发“高质感反馈”：

容器边框/阴影增强（微发光、渐变边框或呼吸光）

光标形态或微提示出现（例如“点击选择场景”）

可选：出现一条“扫光/流光”从边缘划过（1 次，不循环，避免烦躁）

悬停不弹出选项（避免误触与视觉噪音），只做“可点”的明确暗示。

2.3 点击态（Click / Focus）

点击对话框任意区域或聚焦输入框后：

弹出场景选择层：问数 / 问答 / 生成报告

形式建议（二选一）：

浮层 Popover：从输入框上方展开，靠近触发点

内嵌 Chips 区：在输入框上方一行出现三个可选卡片/胶囊按钮

动效建议：

“展开”用轻弹（spring）或渐入 + 位移（8–12px）

选中某个场景时有“选中态过渡”（背景填充、描边高亮、图标微旋转/缩放）
选择场景后：
场景选择层自动收起或缩成一个“当前场景标签”（可再次点击切换）
输入框 placeholder 根据场景变化，提供明确引导
3. 三个场景的行为定义
3.1 问数（偏结构化/数据查询）
文案引导：如“输入指标、时间范围、维度…”
期望输出：表格/数字卡片/趋势摘要（后续可扩展）
UI 暗示：小图标可用“表格/图表/数据”意象
3.2 问答（通用对话）
文案引导：如“直接问我任何问题…”
期望输出：标准对话气泡 + 引用/要点列表
3.3 生成报告（长文本产出）
文案引导：如“描述背景、目标、受众、格式要求…”
期望输出：分段结构化报告（标题、目录、要点、结论）
可选：展示“报告结构提示”或“模板快捷项”（非必需）
4. 输入区（底部输入框）需求
输入框始终存在（减少模式切换的跳变）。
支持：
Enter 发送，Shift+Enter 换行
发送按钮（禁用态、加载态）
清晰的焦点态（focus ring / 微发光）
选中场景后：
placeholder 动态变化
可选：输入框左侧显示当前场景 icon + 名称（点击可切换）
5. 对话区“AI 酷炫思考效果”需求
5.1 触发时机
用户点击发送后 → 进入“AI 思考中”状态，直到首段回复开始渲染或流式输出结束。
5.2 表现形式（建议组合）
思考占位气泡（assistant message skeleton）：
三点呼吸 / 波形跳动 / 光标闪烁
“推理流光”背景层（低对比、不可喧宾夺主）：
在对话气泡或对话区域底层出现轻微粒子流动/网格噪声/扫描线
逐字流式输出（打字机效果）：
适度速度，避免太慢
若是报告场景：可先吐出“结构骨架”再填充细节（更像在思考组织）
5.3 约束
动效必须可被降级：
系统“减少动态效果”开启时自动降低或关闭粒子/扫光
性能要求：
保持 60fps（至少在中高端设备）
粒子/发光强度可配
6. 状态与异常处理
空态：引导选择场景或直接输入
加载态：输入禁用、发送按钮 loading、可中断（可选：Stop 按钮）
错误态：网络/超时/服务错误提示，提供“重试”
切换场景：
未发送前可随时切换
已有对话时切换是否影响上下文：
默认建议：切换只影响下一条消息的处理方式，历史不变
7. 验收要点（可测）
悬停：出现明确但克制的可交互反馈（描边/发光/提示），无误触弹层
点击：场景选项能出现，动效自然，选择后状态明确保留
输入：placeholder 随场景变化，发送逻辑正确
思考效果：发送后立即出现“AI 思考中”，回复开始后自然衔接到流式输出
可访问性：键盘可操作（Tab/Enter）、减少动效可生效
性能：动效不明显掉帧，弱设备可降级